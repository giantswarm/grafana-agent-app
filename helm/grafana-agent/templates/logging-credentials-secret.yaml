{{- if .Values.agent.loggingSecret.enabled -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.agent.loggingSecret.secretName | default "grafana-agent-logging-secret" }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "grafana-agent.labels" . | nindent 4 }}
data:
  logging-url: {{ .Values.agent.loggingSecret.url }}
  logging-tenant-id: {{ .Values.agent.loggingSecret.tenantId }}
  logging-username: {{ .Values.agent.loggingSecret.username }}
  logging-password: {{ .Values.agent.loggingSecret.password }}
{{- end -}}